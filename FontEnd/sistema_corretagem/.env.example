# Variáveis de ambiente do Frontend (Vite)
# Copie este arquivo para .env e preencha conforme seu ambiente.
# NUNCA comite o arquivo .env real.

# URL base da API do backend (incluindo /api/v1)
# Exemplo para desenvolvimento (ajuste a porta conforme seu servidor Rails):
# VITE_API_URL=http://localhost:3001/api/v1
VITE_API_URL=

# Exemplo para produção:
# VITE_API_URL=https://api.seudominio.com/api/v1

# Observações:
# - Não insira segredos, senhas ou tokens neste arquivo — mantenha tudo apenas em .env (e nunca comite o .env real).
# - O frontend usa import.meta.env.VITE_API_URL; se não definido, haverá fallback para http://localhost:3001/api/v1.
# - Recomendo especificar VITE_API_URL explicitamente em dev, test e prod para evitar inconsistências entre ambientes.