# Variáveis de ambiente para desenvolvimento

# Chave secreta para JWT (não colocar valor real aqui)
JWT_SECRET=

# Tempo de vida do JWT em segundos (ex.: 7200 em dev, 900 em prod)
JWT_TTL_SECONDS=

# Outras variáveis importantes (se houver)
RAILS_MASTER_KEY=

# =========================
# Active Storage e Uploads
# =========================
# Tempo de expiração padrão para URLs assinadas do Active Storage (em minutos)
ACTIVE_STORAGE_URLS_EXPIRE_IN_MIN=

# Upload validation
# Tamanho máximo em MB para fotos
MAX_UPLOAD_SIZE_MB=8
# Tipos permitidos (MIME)
ALLOWED_IMAGE_TYPES=image/jpeg,image/png,image/webp

# Processor de variantes de imagem (vips ou mini_magick)
IMAGE_VARIANT_PROCESSOR=vips

# CORS
# Lista de origens permitidas separadas por vírgula (ex.: http://localhost:5173,http://127.0.0.1:5173)
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,http://localhost:5174,http://127.0.0.1:5174,http://localhost:5176,http://127.0.0.1:5176,http://localhost:19006,http://127.0.0.1:19006,http://localhost:8082,http://127.0.0.1:8082,http://localhost:8083,http://127.0.0.1:8083,http://localhost:8084,http://127.0.0.1:8084,http://localhost:8085,http://127.0.0.1:8085,http://localhost:8086,http://127.0.0.1:8086

# Seeds/Admin
# Credenciais do usuário administrador criadas pelo seeds (não colocar valores reais aqui)
ADMIN_EMAIL=
ADMIN_LOGIN=
ADMIN_PASSWORD=

# Jobs/Filas
# Concorrência de jobs em background
JOB_CONCURRENCY=

# Paginação
# Itens por página (Pagy). Ex.: 20 em dev/prod para consistência.
PAGY_ITEMS=

# =========================
# Refresh Tokens e Cookies
# =========================
# Pepper para hash de refresh tokens (não colocar valor real)
REFRESH_TOKEN_PEPPER=
# TTL de refresh em dias (ex.: 30 em prod, 7 em dev)
REFRESH_TOKEN_TTL_DAYS=
# Domínio do cookie por ambiente:
# - Dev: deixar vazio (mesmo-origin via proxy), evita problemas. Ex.: (vazio) ou localhost
# - Staging: .staging.corretorfacil.com
# - Prod: .corretorfacil.com
COOKIE_DOMAIN=
# Política SameSite do cookie (None|Lax|Strict). Em Dev, manter Lax.
COOKIE_SAMESITE=Lax
# Flag Secure: em Dev false; em Staging/Prod true.
COOKIE_SECURE=false

# =========================
# Rack Attack (Rate Limiting)
# =========================
# Limites para /login por IP
RACK_ATTACK_LOGIN_PER_MIN=
# Limites para /login por email (em 10min)
RACK_ATTACK_LOGIN_EMAIL_PER_10MIN=
# Limites para /auth/refresh por device/UA
RACK_ATTACK_REFRESH_PER_MIN=
# Safelist de IPs (separados por vírgula)
RACK_ATTACK_SAFELIST_IPS=

# =========================
# Redis (Tokens & Blacklist)
# =========================
# URL do Redis (ex.: redis://localhost:6379/0 ou rediss://...)
REDIS_URL=
# TLS habilitado para Redis (true|false)
REDIS_TLS=false
# Namespace para chaves de refresh tokens/blacklist
REDIS_REFRESH_NAMESPACE=auth:refresh
# TTL padrão em segundos para refresh tokens no Redis (ex.: 604800 para 7 dias)
REDIS_REFRESH_TTL_SECONDS=